# Viewports are CSS-pixel canvases

A **viewport** is an exact width and height for inspecting composition.
Viewport tokens stay in `primitive.viewport` as `px` dimensions and never
enter StyleX vars.
